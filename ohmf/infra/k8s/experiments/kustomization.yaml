apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gateway-pod-kill.yaml
  - gateway-reconnect-storm-pod-kill.yaml
  - processor-pod-restart.yaml
  - gateway-to-redis-delay.yaml
  - gateway-to-kafka-delay.yaml

commonLabels:
  app.kubernetes.io/part-of: ohmf
  app.kubernetes.io/component: chaos
