OHMF Gap Report — 2026-03-12

Summary
- Performed end-to-end test and CI-style validations after previous fixes.
- Ran unit tests and benchmarks for the `ohmf` module (all tests passed).
- Ran the integration test both locally and inside a Docker Compose network (inside `itest`). Both passed.
- Built service images via `docker compose build` and validated an internal test-runner executed the `TestGatewayIntegration` test successfully.

What I ran
- Local unit tests and benchmarks:
  - `Push-Location ohmf; .\.tools\go\bin\go.exe test ./... -bench . -benchmem`
- Containerized integration test (internal, no host port binding):
  - `docker compose up --build itest`
  - Verified logs: `docker compose logs itest`

Findings / Changes Made
- Fixed a persistent test flake where `integration.TestGatewayIntegration` could receive `409 Conflict` when an app registration already existed:
  - Added unique `app_id` generation in `ohmf/integration/integration_test.go` to avoid collisions.
  - Made the integration test respect `GATEWAY_ADDR` and `CONTACTS_ADDR` environment variables so it can target container-internal service names when run inside Compose.
- Removed a pre-seeded `registered_apps.json` file that caused deterministic conflicts during integration runs.
- Adjusted Dockerfiles to place service binaries into `/bin/<name>` to avoid path conflicts with directories inside containers.
- Added a Compose-only test-runner service `itest` to run the integration test inside the compose network (avoids binding host ports and local port conflicts).

Current Status
- All unit tests for `ohmf` pass locally with the repository-local Go toolchain.
- Benchmarks ran (if present) as part of `go test -bench . -benchmem` invocation; none failed.
- Containerized integration test (`itest`) passed inside the compose network.

Remaining Recommendations (gaps)
- CI Integration: ensure GitHub Actions or CI uses the same containerized test-runner pattern (run integration tests inside compose network rather than binding host ports). I added `itest` to `docker-compose.yml`, but CI workflow should run `docker compose up --build itest` (or equivalent) and fail if `itest` exits non-zero.
- Compose host-port hygiene: current `docker-compose.yml` uses `expose` for services to avoid host conflicts; if host port bindings are desired for local dev, document expected default ports and provide an `override` compose file.
- Persistent state: consider replacing file-backed app registrations with ephemeral or test-isolated storage for CI runs; currently the service writes to a JSON file by default. For production, introduce a persistent data store (SQLite/Postgres) or configure `DATA_FILE` via env in deployments.
- Health & readiness: strengthen healthchecks and add a `wait-for` script in CI that waits for service readiness before running integration tests. Current healthchecks exist but CI orchestration should check them before running `itest`.
- OpenAPI and spec conformance: run `validate_openapi` (provided in `validate_openapi/`) in CI to ensure OpenAPI stays in sync with implementation.

Files changed during this work (high level)
- `ohmf/integration/integration_test.go` — unique app_id, env-driven addresses
- `ohmf/ohmf/services/apps/data/registered_apps.json` — removed (pre-seeded file)
- `ohmf/services/media/Dockerfile` — binary installed to `/bin/media`
- `docker-compose.yml` — added `itest` test-runner, changed to `expose` internal ports

Next Steps
- (High priority) If you want, I can create a branch with these changes and open a PR including the updated tests, Dockerfiles, and `docker-compose.yml` modifications so CI can run these validations.
- (Optional) Update CI workflow to use the `itest` approach and run `validate_openapi` as part of the pipeline.

If you'd like the PR opened now, I can create a branch, commit the modified files, push, and open the PR.