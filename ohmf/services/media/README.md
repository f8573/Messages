Media service scaffold

Provides an endpoint to initialize uploads and return short-lived upload URLs/tokens.

Planned endpoints:
- POST /v1/media/uploads — return upload_id and upload_url
- POST /v1/media/uploads/{upload_id}/complete — confirm upload

Next steps:
- Integrate with S3-compatible storage and verify upload signatures.
# Future Service Placeholder

This directory is reserved as a future extraction target.
