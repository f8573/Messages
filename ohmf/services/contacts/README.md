Contacts service (scaffold)

This directory contains a minimal scaffold for the OHMF Contacts / discovery service.

Planned endpoints:
- POST /v1/contacts/discover — privacy-preserving discovery (hash-based or PSI)

Next steps:
- Implement hashing/pepper strategy and rate limiting
- Wire into gateway routing and add tests
