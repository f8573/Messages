package version

// APIVersion is the current HTTP API version served by the gateway.
const APIVersion = "v1"

// SpecVersion is the canonical OHMF spec version used in envelopes and events.
const SpecVersion = "2026-03-01"
